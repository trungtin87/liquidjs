---
title: setup
---

Hướng dẫn cài đặt LiquidJS và cấu hình cơ bản.

1. Cài đặt bằng npm: `npm install liquidjs`
2. Tạo instance: `const engine = new Liquid()`

Ví dụ:
```javascript
const { Liquid } = require('liquidjs')
const engine = new Liquid()
```
